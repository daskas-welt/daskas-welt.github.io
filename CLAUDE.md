# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build and Development Commands

This is a static website. No build step is required.

- Formatting: `npx prettier --write .`

## Architecture and Structure

- `index.html`: Main entry point and structure.
- `style.css`: Custom styling, extending Bootstrap 5.
- `img/`: Assets and images.

The project is a simple static site using Bootstrap 5 and custom CSS.
